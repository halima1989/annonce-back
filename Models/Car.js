class Car {
  constructor(image, model, brand, description, price) {
    image = this.image;
    model = this.model;
    brand = this.brand;
    description = this.description;
    price = this.price;
  }
}

module.exports = { Car };
