class Car {
  constructor(name, image, model, brand, description, price) {
    name = this.name;
    image = this.image;
    model = this.model;
    brand = this.brand;
    description = this.description;
    price = this.price;
  }
}

module.exports = { Car };
